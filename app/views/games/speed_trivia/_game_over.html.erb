<%# app/views/games/speed_trivia/_game_over.html.erb %>

<header class="max-w-md mx-auto mb-6 px-2">
  <span class="text-yellow-400 text-xs font-bold tracking-widest uppercase">Game Over</span>
</header>

<div class="max-w-md mx-auto">
  <div class="bg-white/10 backdrop-blur-md rounded-3xl p-8 border border-yellow-400/30 text-center">
    <% rank = room.players.active_players.order(score: :desc).pluck(:id).index(player.id)&.+ 1 || 0 %>

    <% if rank == 1 %>
      <div class="text-6xl mb-4">🏆</div>
      <p class="text-3xl text-yellow-400 font-black mb-2">You Won!</p>
    <% elsif rank <= 3 %>
      <div class="text-6xl mb-4"><%= ["🥈", "🥉"][rank - 2] %></div>
      <p class="text-3xl text-white font-bold mb-2"><%= rank.ordinalize %> Place</p>
    <% else %>
      <div class="text-6xl mb-4">🎮</div>
      <p class="text-3xl text-white font-bold mb-2"><%= rank.ordinalize %> Place</p>
    <% end %>

    <p class="text-5xl text-yellow-400 font-black font-mono my-6"><%= player.score %></p>
    <p class="text-lg text-blue-200">points</p>
  </div>

  <!-- Mini Leaderboard -->
  <div class="mt-6 bg-white/5 backdrop-blur-md rounded-2xl p-4 border border-white/10">
    <h3 class="text-sm text-blue-200 font-bold uppercase tracking-wider mb-3">Standings</h3>
    <div class="space-y-2">
      <% room.players.active_players.order(score: :desc).limit(5).each_with_index do |p, index| %>
        <div class="flex items-center justify-between <%= p.id == player.id ? 'bg-blue-900/40 rounded-lg px-2 py-1' : '' %>">
          <div class="flex items-center gap-2">
            <span class="text-gray-400 font-bold w-6">#<%= index + 1 %></span>
            <span class="text-white font-medium <%= p.id == player.id ? 'font-bold' : '' %>"><%= p.name %></span>
          </div>
          <span class="text-yellow-400 font-bold font-mono"><%= p.score %></span>
        </div>
      <% end %>
    </div>
  </div>
</div>
