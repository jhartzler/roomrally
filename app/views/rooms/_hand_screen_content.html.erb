  <% if room.current_game.present? %>
    <%= render "games/#{room.game_type.parameterize.underscore}/hand", room: room, player: player %>
  <% else %>
    <%= render "rooms/lobby", room: room, player: player %>
  <% end %>
